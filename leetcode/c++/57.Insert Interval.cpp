class Solution {
public:
    vector<vector<int>> insert(vector<vector<int>>& intervals, vector<int>& newInterval) {
        vector<vector <int>> ret;
        int cnt = 0;
        int flag = 0;
        for(int i = 0; i < intervals.size(); i++)
        {
            //check first value
            if(intervals[i][0] >= newInterval[1])    // ][]
            {
                
            }
            else if(intervals[i][0] <= newInterval[
                
            ])
            {
                
            }
                
            //add ret
            ret[i][0] =             
        }
    }
};